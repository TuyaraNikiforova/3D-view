<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Детализация</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #000000;
            color: #ffffff;
        }
        
        .dashboard-container {
            max-width: 1200px;
            margin: 0 auto;
            background-color: #1a1a1a;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 2px 10px rgba(255,255,255,0.1);
        }
        
        .dashboard-header {
            display: flex;
            gap: 15px;
            margin-bottom: 20px;
            flex-wrap: wrap;
        }
      
        .export-btn, .filter-btn, .clear-applied-btn, .clear-all-btn {
            padding: 10px 20px;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 14px;
            transition: background-color 0.3s;
        }
        
        .export-btn {
            background-color: #4a6da7;
        }
        
        .export-btn:hover {
            background-color: #3a5a8f;
        }
        
        .filter-btn {
            background-color: #4a6da7; /* Синий цвет как у экспорта */
        }
        
       
		.clear-all-btn {
            background-color: #8a8a8a; 
        }
		
        .clear-applied-btn {
            background-color: #8a8a8a; 
        }
	
        .filters-container {
            display: flex;
            flex-direction: row;
            gap: 15px;
            padding: 15px;
            background-color: #2a2a2a;
            border-radius: 8px;
            margin-bottom: 20px;
            flex-wrap: wrap;
        }
        
        .filter-group {
            flex: 1;
            min-width: 280px;
            background-color: #333;
            padding: 15px;
            border-radius: 6px;
            box-shadow: 0 2px 4px rgba(255,255,255,0.1);
        }
        
        .filter-group h4 {
            margin: 0 0 10px 0;
            color: #4a6da7;
        }
        
        .table-container {
            margin-bottom: 20px;
            border: 1px solid #444;
            border-radius: 4px;
            overflow: hidden;
        }
        
        .table-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px 15px;
            background-color: #4a6da7;
            color: white;
            cursor: pointer;
        }
                
        .table-content {
            overflow-x: auto;
            width: 100%;
            background-color: #2a2a2a;
        }
        
        .dashboard-table {
            border-collapse: collapse;
            width: 100%;
            margin-bottom: 20px;
            color: #ddd;
        }
                
        .dashboard-table th,
        .dashboard-table td {
            border: 1px solid #444;
            padding: 8px;
            text-align: left;
        }
                
        .dashboard-table th {
            background-color: #3a3a3a;
            font-weight: bold;
            color: #fff;
        }

        .dashboard-table tr:nth-child(even) {
            background-color: #2f2f2f;
        }
                
        .dashboard-table tr:hover {
            background-color: #3a3a3a;
        }
        
        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: #888;
        }

        .empty-state h3 {
            margin-bottom: 15px;
            color: #ccc;
        }

        .empty-state p {
            font-size: 16px;
            line-height: 1.5;
        }
                
        .checkmark {
            color: #6da74a;
            font-weight: bold;
            font-size: 16px;
        }
        
        .status-message {
            padding: 10px;
            margin: 10px 0;
            border-radius: 4px;
            text-align: center;
        }
        
        .status-error {
            background-color: #ffebee;
            color: #c62828;
            border: 1px solid #ef9a9a;
        }
        
        .status-success {
            background-color: #e8f5e9;
            color: #2e7d32;
            border: 1px solid #a5d6a7;
        }
        
        .filter-dropdown {
            margin-bottom: 10px;
        }
        
        .filter-dropdown-header {
            cursor: pointer;
            padding: 8px;
            background-color: #444;
            border-radius: 4px;
            margin-bottom: 5px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .filter-dropdown-content {
            display: none;
            max-height: 200px;
            overflow-y: auto;
            background-color: #333;
            padding: 10px;
            border-radius: 4px;
        }

        .filter-checkbox {
            margin: 5px 0;
        }

        .filter-checkbox label {
            display: flex;
            align-items: center;
            cursor: pointer;
        }
        
        .filter-checkbox input {
            margin-right: 8px;
        }

        .filter-checkbox:hover {
            background-color: #3a3a3a;
            padding: 2px 5px;
            border-radius: 3px;
        }
        
        .filter-buttons {
            margin-top: 15px;
            display: flex;
            gap: 10px;
        }

        #clear-filters {
            background-color: #8a8a8a;
            color: white;
        }
        
        .filter-buttons button {
            padding: 8px 16px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 14px;
        }

.filter-section {
    margin-bottom: 20px;
}

.filter-section h3 {
    color: #4a6da7;
    margin-bottom: 10px;
}

.modal-filter-content {
    max-height: 200px;
    overflow-y: auto;
    background-color: #333;
    padding: 10px;
    border-radius: 4px;
}

.modal-filter-content .filter-checkbox {
    margin: 5px 0;
}

.modal-filter-content .filter-checkbox label {
    display: flex;
    align-items: center;
    cursor: pointer;
}

.modal-filter-content .filter-checkbox input {
    margin-right: 8px;
}

.modal-filter-content .filter-checkbox:hover {
    background-color: #3a3a3a;
    padding: 2px 5px;
    border-radius: 3px;
}
		
.table-filters {
    display: flex;
    gap: 15px;
    padding: 10px;
    background-color: #333;
    border-bottom: 1px solid #555;
    margin-bottom: 10px;
}

.table-filters label {
    display: flex;
    align-items: center;
    gap: 5px;
    color: #ccc;
    cursor: pointer;
}

.table-filters button {
    padding: 5px 10px;
    background-color: #4a6da7;
    color: white;
    border: none;
    border-radius: 4px;
    cursor: pointer;
}		
    </style>
</head>
<body>
    <div class="dashboard-container" id="dashboard-container">
        <div class="dashboard-header">
            <button class="export-btn" id="export-btn">Экспорт в Excel</button>
            <button class="filter-btn" id="apply-filters">Применить фильтры</button>
            <button class="clear-applied-btn" id="clear-applied-filters">Очистить примененный фильтр</button>
            <button class="clear-all-btn" id="clear-filters">Очистить все фильтры</button>
        </div>
        
        <div id="status-area"></div>
        
        <div class="table-container" id="themes-table-container">
            <div class="table-header">
                <h3>Темы</h3>
            </div>
            <div class="table-content" id="themes-table-content"></div>
        </div>
        
        <div class="table-container" id="objects-table-container">
            <div class="table-header">
                <h3>Объекты управления</h3>
            </div>			
            <div class="table-content" id="objects-table-content"></div>		
        </div>
        
        <div class="table-container" id="parameters-table-container">
            <div class="table-header">
                <h3>Параметры объектов управления</h3>
            </div>		
            <div class="table-content" id="parameters-table-content"></div>
        </div>
        
        <div class="table-container" id="indicators-table-container">
            <div class="table-header">
                <h3>Показатели</h3>
            </div>			
            <div class="table-content" id="indicators-table-content"></div>
        </div>
    </div>

    <!-- Загружаем библиотеку XLSX с правильного CDN -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>
    <script src="/js/dashboard.js"></script>
</body>
</html>